#include <iostream>

/**
 * @brief This is a test function.
 * 
 * This test function has far too many paramters, and returns a template param.
 * 
 * Here is a code block `Code Stuffs`
 * ```cpp
 * void main();
 * ```
 *
 * @tparam T Type of `A`.
 * @tparam B A class object of `B`
 * @param arga First argument term
 * @param argb Second argument term
 * @param c The cth arg.
 * @param d hello.
 * @param e good bye
 * @param f whealajsdf;
 * @param g lajdf;kasjdsf 
 * @param h lkslajdf;a
 * @param i This is more *Code* in **CODE*
 *
 * @return A type `B` value of __stuff__.
 */
template <typename T, class B>
B test(B arga, T argb, int c, unsigned int d, unsigned int e, unsigned int f,
       unsigned int g, unsigned int h, unsigned int i) {
  return;
}
